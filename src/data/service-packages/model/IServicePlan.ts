export interface IServicePlan {
    name: string,
    price: string,
    hourlyEquivalent: string,
    hours?: string,
    schedule?: string,
    highlight: string,
    features: string[],
    link: string,
}