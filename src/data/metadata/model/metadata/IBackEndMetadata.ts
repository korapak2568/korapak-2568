import {Metadata} from "next";

export interface IBackEndMetadata {
    main: Metadata,
    dotnetcore: Metadata,
    go: Metadata,
    java: Metadata,
    nodejs: Metadata,
    php: Metadata,
    python: Metadata,
}