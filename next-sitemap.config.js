module.exports = {
    siteUrl: process.env.SITE_URL || 'https://chorn.in.th',
    generateRobotsTxt: true, // Optionally generate robots.txt
    changefreq: 'daily',
    priority: 0.7,
    sitemapSize: 5000,
};